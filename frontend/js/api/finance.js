/* ==========================================================================
   api/finance.js
   Placeholder for the real finance backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listFinance() {
//   // TODO: const res = await fetch('/api/finance'); return res.json();
// }

// export async function getFinanceById(id) {
//   // TODO: const res = await fetch(`/api/finance/${id}`); return res.json();
// }

// export async function createFinance(payload) {
//   // TODO: const res = await fetch('/api/finance', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateFinance(id, payload) {
//   // TODO: const res = await fetch(`/api/finance/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
