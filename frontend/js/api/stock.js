/* ==========================================================================
   api/stock.js
   Placeholder for the real stock backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listStock() {
//   // TODO: const res = await fetch('/api/stock'); return res.json();
// }

// export async function getStockById(id) {
//   // TODO: const res = await fetch(`/api/stock/${id}`); return res.json();
// }

// export async function createStock(payload) {
//   // TODO: const res = await fetch('/api/stock', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateStock(id, payload) {
//   // TODO: const res = await fetch(`/api/stock/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
