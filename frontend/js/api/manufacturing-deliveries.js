/* ==========================================================================
   api/manufacturing-deliveries.js
   Placeholder for the real manufacturing-deliveries backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listManufacturingDeliveries() {
//   // TODO: const res = await fetch('/api/manufacturing-deliveries'); return res.json();
// }

// export async function getManufacturingDeliveriesById(id) {
//   // TODO: const res = await fetch(`/api/manufacturing-deliveries/${id}`); return res.json();
// }

// export async function createManufacturingDeliveries(payload) {
//   // TODO: const res = await fetch('/api/manufacturing-deliveries', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateManufacturingDeliveries(id, payload) {
//   // TODO: const res = await fetch(`/api/manufacturing-deliveries/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
