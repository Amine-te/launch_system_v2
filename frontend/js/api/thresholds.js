/* ==========================================================================
   api/thresholds.js
   Placeholder for the real thresholds backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listThresholds() {
//   // TODO: const res = await fetch('/api/thresholds'); return res.json();
// }

// export async function getThresholdsById(id) {
//   // TODO: const res = await fetch(`/api/thresholds/${id}`); return res.json();
// }

// export async function createThresholds(payload) {
//   // TODO: const res = await fetch('/api/thresholds', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateThresholds(id, payload) {
//   // TODO: const res = await fetch(`/api/thresholds/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
