/* ==========================================================================
   api/customer-deliveries.js
   Placeholder for the real customer-deliveries backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listCustomerDeliveries() {
//   // TODO: const res = await fetch('/api/customer-deliveries'); return res.json();
// }

// export async function getCustomerDeliveriesById(id) {
//   // TODO: const res = await fetch(`/api/customer-deliveries/${id}`); return res.json();
// }

// export async function createCustomerDeliveries(payload) {
//   // TODO: const res = await fetch('/api/customer-deliveries', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateCustomerDeliveries(id, payload) {
//   // TODO: const res = await fetch(`/api/customer-deliveries/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
