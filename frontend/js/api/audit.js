/* ==========================================================================
   api/audit.js
   Placeholder for the real audit backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listAudit() {
//   // TODO: const res = await fetch('/api/audit'); return res.json();
// }

// export async function getAuditById(id) {
//   // TODO: const res = await fetch(`/api/audit/${id}`); return res.json();
// }

// export async function createAudit(payload) {
//   // TODO: const res = await fetch('/api/audit', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateAudit(id, payload) {
//   // TODO: const res = await fetch(`/api/audit/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
