/* ==========================================================================
   api/admin.js
   Placeholder for the real admin backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listAdmin() {
//   // TODO: const res = await fetch('/api/admin'); return res.json();
// }

// export async function getAdminById(id) {
//   // TODO: const res = await fetch(`/api/admin/${id}`); return res.json();
// }

// export async function createAdmin(payload) {
//   // TODO: const res = await fetch('/api/admin', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateAdmin(id, payload) {
//   // TODO: const res = await fetch(`/api/admin/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
