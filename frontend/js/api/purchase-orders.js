/* ==========================================================================
   api/purchase-orders.js
   Placeholder for the real purchase-orders backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listPurchaseOrders() {
//   // TODO: const res = await fetch('/api/purchase-orders'); return res.json();
// }

// export async function getPurchaseOrdersById(id) {
//   // TODO: const res = await fetch(`/api/purchase-orders/${id}`); return res.json();
// }

// export async function createPurchaseOrders(payload) {
//   // TODO: const res = await fetch('/api/purchase-orders', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updatePurchaseOrders(id, payload) {
//   // TODO: const res = await fetch(`/api/purchase-orders/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
