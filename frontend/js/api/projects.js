/* ==========================================================================
   api/projects.js
   Placeholder for the real projects backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listProjects() {
//   // TODO: const res = await fetch('/api/projects'); return res.json();
// }

// export async function getProjectsById(id) {
//   // TODO: const res = await fetch(`/api/projects/${id}`); return res.json();
// }

// export async function createProjects(payload) {
//   // TODO: const res = await fetch('/api/projects', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateProjects(id, payload) {
//   // TODO: const res = await fetch(`/api/projects/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
