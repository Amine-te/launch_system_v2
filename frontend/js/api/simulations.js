/* ==========================================================================
   api/simulations.js
   Placeholder for the real simulations backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listSimulations() {
//   // TODO: const res = await fetch('/api/simulations'); return res.json();
// }

// export async function getSimulationsById(id) {
//   // TODO: const res = await fetch(`/api/simulations/${id}`); return res.json();
// }

// export async function createSimulations(payload) {
//   // TODO: const res = await fetch('/api/simulations', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateSimulations(id, payload) {
//   // TODO: const res = await fetch(`/api/simulations/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
