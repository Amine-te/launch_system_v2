/* ==========================================================================
   api/boms.js
   Placeholder for the real boms backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listBoms() {
//   // TODO: const res = await fetch('/api/boms'); return res.json();
// }

// export async function getBomsById(id) {
//   // TODO: const res = await fetch(`/api/boms/${id}`); return res.json();
// }

// export async function createBoms(payload) {
//   // TODO: const res = await fetch('/api/boms', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateBoms(id, payload) {
//   // TODO: const res = await fetch(`/api/boms/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
