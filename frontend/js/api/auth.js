/* ==========================================================================
   api/auth.js
   Placeholder for the real auth backend endpoints. Not implemented yet --
   the app currently reads/writes in-memory mock data from js/data/mock-data.js
   and js/state.js. Swap the bodies below for real fetch() calls once the
   backend exists; keep the same function names/signatures so callers don't
   need to change.
   ========================================================================== */

// export async function listAuth() {
//   // TODO: const res = await fetch('/api/auth'); return res.json();
// }

// export async function getAuthById(id) {
//   // TODO: const res = await fetch(`/api/auth/${id}`); return res.json();
// }

// export async function createAuth(payload) {
//   // TODO: const res = await fetch('/api/auth', { method: 'POST', body: JSON.stringify(payload) }); return res.json();
// }

// export async function updateAuth(id, payload) {
//   // TODO: const res = await fetch(`/api/auth/${id}`, { method: 'PATCH', body: JSON.stringify(payload) }); return res.json();
// }
