# --- Web framework (installed now, no endpoints yet) ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Config ---
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
email-validator==2.2.0

# --- Auth (login endpoint: password hashing + JWTs) ---
# bcrypt pinned <4.1: passlib 1.7.4 reads bcrypt.__about__.__version__,
# which newer bcrypt releases removed, breaking hashing at runtime.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.10.1
